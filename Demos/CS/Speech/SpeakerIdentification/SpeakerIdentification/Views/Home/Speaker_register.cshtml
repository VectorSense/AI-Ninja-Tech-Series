<!DOCTYPE html>
<html lang="en-us">
<head>
    <meta charset="utf-8" />
    <!-- Page Title -->
    <title>Speaker Identification</title>
    <meta name="keywords" content="" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <!-- Mobile Meta Tag -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
    <!-- Fav and touch icons -->
    <link rel="shortcut icon" type="image/x-icon" href="/assets/images/fav_touch_icons/favicon.ico" />
    <link rel="apple-touch-icon" href="/assets/images/fav_touch_icons/apple-touch-icon.png" />
    <link rel="apple-touch-icon" sizes="72x72" href="/assets/images/fav_touch_icons/apple-touch-icon-72x72.png" />
    <link rel="apple-touch-icon" sizes="114x114" href="/assets/images/fav_touch_icons/apple-touch-icon-114x114.png" />
    <!-- Fonf Awesome -->
    <link rel="stylesheet" type="text/css" href="~/assets/css/font-awesome.min.css" />
    <link rel="stylesheet" type="text/css" href="~/assets/css/bootstrap.min.css" />
    <!-- Template CSS -->
    <link rel="stylesheet" type="text/css" href="~/assets/css/style.css" />
    <style>

        body {
            font-family: 'Segoe UI',SegoeUI,'Segoe WP',Tahoma,Arial,sans-serif;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Segoe UI',SegoeUI,'Segoe WP',Tahoma,Arial,sans-serif;
            text-transform: none;
        }

        .btn {
            text-transform: none;
        }

        .take-pic {
            margin-top: 3%;
            width: 100%;
            height: 345px;
        }

        audio, canvas, progress, video {
            display: inline-block;
            vertical-align: baseline;
        }

        #check_id {
            margin-left: 25%;
        }

        .signin-btn {
            background: #036;
            color: #dfdada;
            border-radius: 20px;
            width: 135px;
            padding: 10px;
            cursor: pointer;
            margin-bottom: 3%;
        }

        input[type=text], input[type=password] {
            height: 40px !important;
        }

        #add-member-details {
            margin-top: 20px;
        }

        ::placeholder {
            color: #000000;
            opacity: 1; /* Firefox */
        }

        .rounded-circle {
            border-radius: 50% !important;
        }
    </style>
</head>
<body class="home-page">
    <!-- BEGIN HEADER -->
    <header id="header" class="header-default">
        <div class="container navbar-header">
            <div class="row">
                <div class="col-md-12">
                    <div class="col-md-3 header-left">
                        <a href="index" class="logo"><img src="/assets/images/microsoft.png" alt="" style="width: auto;margin-top: 11%;margin-left: -25%;"></a>
                    </div>
                    <div class="col-md-9 header-right">
                        <h2 style="text-align: left;margin-left:12%;color: #0477d3;margin-bottom: 10px;font-size: 52px;text-transform:uppercase;font-weight:600; font-family:" open="" sans",="" sans-serif;"="">Partner Tech Series</h2>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <!-- END HEADER -->
    <br>
    <!-- BEGIN SITE MAP -->
    <div class="site-map">
        <div class="container">
            <a href="index">Home</a>
            <a href="#">Register</a>
        </div>
    </div>
    <!-- END SITE MAP -->
    <!-- BEGIN MAIN CONTENT -->
    <div class="row">
        <div class="col-md-2 col-sm-2"></div>
        <div class="col-md-4 col-sm-4">
            <h3 style="padding-top: 100px;">Registration</h3>
            <br>
            <label>Your Name</label>
            <input type="text" name="user_name" class="form-control" id="user_name" placeholder="Enter your name">
            <label>Audio file upload (.wav)</label>
            <input type="file" name="audio_file" class="form-control" id="audio_file">
            <button style="margin-top: -2px;color:white;" type="button" class="btn signin-btn" id="registration_submit" data-loading-text="<i class='fa fa-spinner fa-spin '></i> Loading..">Register</button>
            <p id="err_msg" style="color:red;font-size: 12px;"></p>
        </div>
        <div class="col-md-1 col-sm-1"></div>
        <div class="col-md-3 col-sm-3">
            <p style="padding-top: 200px;text-align: center;font-weight: bolder;font-size: 20px;" id="reg_output"></p>
        </div>
    </div>
    <!-- END MAIN CONTENT -->
    <!--BEGIN FOOTER -->
    <footer id="footer" class="footer-background" style="margin-top: 180px;">
        <!--BEGIN COPYRIGHT -->
        <div id="copyright">
            <!--  <a href="#" class="scrollTopButton">
               <span class="button-square">
                   <i class="fa fa-angle-double-up"></i>
               </span>
               </a> -->
            <div class="container">
                <div class="row">
                    <div class="col-sm-12">
                        <span class="allright">2019 <strong class="t-color">Jumpstartninja </strong> All rights reserved.</span>
                    </div>
                </div>
            </div>
        </div>
        <!-- END COPYRIGHT -->
    </footer>
    <div class="modal fade" id="alert_popup" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-body">
                    <center>
                        <h4 id="modal_error_msg" style="color:black;"></h4>
                        <br><a href="/Home/Speaker_register"><button class="btn-md btn-primary" id="refresh_btn">Okay</button></a>
                    </center>
                </div>
            </div>
        </div>
    </div>
    <!-- END FOOTER -->
    <!-- END WRAPPER -->
    <!-- Libs -->
    <script type="text/javascript" src="~/assets/js/common/jquery-1.x-git.min.js"></script>
    <script type="text/javascript" src="~/assets/js/common.js"></script>
    <script type="text/javascript" src="~/assets/js/bootstrap-progressbar.js"></script>
    <script type="text/javascript" src="~/assets/js/owl.carousel.min.js"></script>
    <!-- Datepiker Script -->
    <script type="text/javascript" src="~/assets/js/datepiker/js/moment.js"></script>
    <script type="text/javascript" src="~/assets/js/datepiker/js/bootstrap-datepicker.js"></script>
    <!-- Template Scripts -->
    <script type="text/javascript" src="~/assets/js/scripts.js"></script>
    <script type="text/javascript" src="~/assets/style-switcher/js/jQuery.style.switcher.js"></script>
    <script type="text/javascript">
    var source = null;
        var audioBuffer = null;
        var base64String = null;
        var bufferToBase64 = function (buffer) {
            var bytes = new Uint8Array(buffer);
            var len = buffer.byteLength;
            var binary = "";
            for (var i = 0; i < len; i++) {
                binary += String.fromCharCode(bytes[i]);
            }
            return window.btoa(binary);
        };
        var base64ToBuffer = function (buffer) {
            var binary = window.atob(buffer);
            var buffer = new ArrayBuffer(binary.length);
            var bytes = new Uint8Array(buffer);
            for (var i = 0; i < buffer.byteLength; i++) {
                bytes[i] = binary.charCodeAt(i) & 0xFF;
            }
            return buffer;
        };
        function initSound(arrayBuffer) {
            base64String = bufferToBase64(arrayBuffer);
            var audioFromString = base64ToBuffer(base64String);
        }
        var fileInput = document.querySelector('input[type="file"]');
        fileInput.addEventListener('change', function (e) {
            var reader = new FileReader();
            reader.onload = function (e) {
                initSound(this.result);
            };
            reader.readAsArrayBuffer(this.files[0]);
        }, false);


        $('#registration_submit').click(function () {
            var user_name = $('#user_name').val();
            if (user_name != "" && base64String !== null) {
                $('#registration_submit').button('loading');
                $('#err_msg').html("");
                var JsonVoice = JSON.stringify({ data: base64String, name: user_name });
                $.ajax({
                    type: "POST",
                    contentType: "application/json",
                    datatype: "json",
                    data: JsonVoice,
                    url: "/Home/SpeakerRegistration",
                    success: function (data) {
                        console.log(data);
                        if (data.Error == "") {
                            $('#reg_output').html(data.Result);
                            $('#registration_submit').button('reset');
                        }
                        else {
                            $("#modal_error_msg").html(data.Error);
                            $("#alert_popup").modal('show');
                            $('#registration_submit').button('reset');
                        }

                    }
                });
            }
            else {
                $('#err_msg').html("Kindly Please fill all the fields");
                $('#registration_submit').button('reset');
            }
        });</script>
</body>
</html>